---
- name: Ensure we are using pacman
  ansible.builtin.fail:
    msg: "This role only supports {{ ansible_os_family }} systems that use pacman as a package manager."
  when: ansible_pkg_mgr != "pacman"

- name: Install lazygit via pacman
  when: ansible_pkg_mgr == "pacman"
  block:
    - name: Update pacman cache
      community.general.pacman:
        update_cache: true
      become: true

    - name: Ensure lazygit is installed
      community.general.pacman:
        name: "{{ package }}"
        state: present
      loop: "{{ lazygit_distro_pkgs }}"
      loop_control:
        loop_var: package
      become: true
